[build-system]
requires = [
    "setuptools>=45",
    "wheel",
    "setuptools_scm[toml]>=6.0"
]
build-backend = 'setuptools.build_meta'

[tool.setuptools_scm]
#write_to = 'matryoshka_tester/version.py'

[tool.pytest.ini_options]
# git marker allow to pass git repo data to tests/fixtures
markers = [
    'git',
]